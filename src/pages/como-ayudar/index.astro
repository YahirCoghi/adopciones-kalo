---
import Layout from '../../layouts/Layout.astro';
import DonationCard from '../../components/DonationCard.astro';
---

<Layout title="Cómo Ayudar / Donar | Adopciones Kalö">
  <section class="container py-12">
    <div class="mb-8">
      <p class="text-xs font-bold uppercase tracking-[0.2em] text-[#7cb5a0]">Cómo ayudar</p>
      <h1 class="mt-2 text-4xl font-extrabold">Tu apoyo salva vidas</h1>
      <p class="mt-3 max-w-2xl text-[#6f625d]">Cada aporte se transforma en alimento, medicinas, esterilizaciones y nuevas oportunidades.</p>
    </div>

    <section class="mb-10">
      <h2 class="mb-4 text-2xl font-bold">Donar</h2>
      <div class="grid gap-4 md:grid-cols-3">
        <DonationCard titulo="SINPE MÓVIL" valor="8672-5678" copyValue="8672-5678" buttonText="Copiar SINPE" />
        <DonationCard titulo="Cuenta BAC" valor="943408567" copyValue="943408567" buttonText="Copiar cuenta BAC" />
        <DonationCard titulo="Cédula Jurídica" valor="3002790325" copyValue="3002790325" buttonText="Copiar cédula jurídica" />
      </div>
      <div class="surface mt-4 p-5 text-sm text-[#5f5652]">
        <p><strong>Nombre:</strong> Asociación Adopciones Kalö</p>
        <p class="mt-2 text-base font-bold text-[#dd5f4a]">¡Gracias por tu apoyo!</p>
      </div>
    </section>

    <section class="grid gap-5 md:grid-cols-3">
      <article class="surface p-5">
        <h3 class="text-lg font-bold">Donación en especie</h3>
        <p class="mt-2 text-sm text-[#6f625d]">Alimento, camitas, cobijas, correas, implementos de limpieza y medicina básica.</p>
      </article>
      <article class="surface p-5">
        <h3 class="text-lg font-bold">Patrocinar rifas</h3>
        <p class="mt-2 text-sm text-[#6f625d]">Puedes donar premios, difundir rifas y apoyarnos a recaudar fondos para rescates.</p>
      </article>
      <article class="surface p-5">
        <h3 class="text-lg font-bold">Compartir publicaciones</h3>
        <p class="mt-2 text-sm text-[#6f625d]">Ayúdanos a llegar a más personas compartiendo perritos en adopción en tus redes.</p>
      </article>
    </section>
  </section>

  <script is:inline>
    const buttons = document.querySelectorAll('[data-copy]');

    const copyToClipboard = async (text) => {
      if (navigator.clipboard?.writeText) {
        await navigator.clipboard.writeText(text);
        return;
      }
      const temp = document.createElement('textarea');
      temp.value = text;
      document.body.append(temp);
      temp.select();
      document.execCommand('copy');
      temp.remove();
    };

    buttons.forEach((button) => {
      button.addEventListener('click', async () => {
        const originalText = button.textContent;
        try {
          await copyToClipboard(button.dataset.copy || '');
          button.textContent = 'Copiado';
        } catch {
          button.textContent = 'No se pudo copiar';
        }
        setTimeout(() => {
          button.textContent = originalText;
        }, 1500);
      });
    });
  </script>
</Layout>

